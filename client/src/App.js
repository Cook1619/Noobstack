import React, { Component } from "react";
//adding to commit
class App extends Component {
  render() {
    return <div>Install Atlas, its a mongo db tool were each gonna have to have our own local db until we go to heroku or firebase</div>;
  }
}

export default App;
